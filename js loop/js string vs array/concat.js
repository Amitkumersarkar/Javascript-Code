const first = 'amit';
const last = 'amrit';
// const fullName = first + last;
// white space in the middle of the name
const fullName = first + ' ' + last;
// console.log(fullName);
const fullName2 = first.concat(' ').concat(last);
console.log(fullName2);

// includes

// console.log(last.includes('d'));
console.log(last.includes('a'));